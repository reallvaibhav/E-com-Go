# Example: order-service/Dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o order-service ./cmd  # Build from cmd directory

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/order-service .
CMD ["./order-service"]